[package]
name = "agent-grid"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = { version = "0.30.0", features = ["init-if-needed"] }
anchor-serum-dex = { version = "0.30.0", optional = true }
solana-program = "1.18"
thiserror = "1.0"

[features]
default = []
devnet = []
